[workspace]
members = [
    "nostd-msvc-build",
    "nostd-msvc-runtime",
]
resolver = "2"

[workspace.package]
license = "0BSD"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
